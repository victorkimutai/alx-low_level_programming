#include "main.h"

/**
 * mul -Multiply two digits
 * @a: Carries the first number
 * @b: Carries the second number
 * Return: It returns the multiplication of two numbers
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
