#include "main.h"
/**
 * _isupper - Check if the letter is lowercase
 * @c - Its a variable to carry the value
 * Return: void (success)
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'a')
{
return (1);
}
else
{
return (0);
}
}

